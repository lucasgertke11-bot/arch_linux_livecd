# Users Configuration for Calamares

# Default user settings
defaultUserName=usuario
defaultUserFullName=Usuario
defaultUserShell=/bin/bash

# Password settings
defaultPassword=
passwordAlgorithm=sha512

# Groups
defaultGroups=wheel,users,audio,video,storage,disk,optical

# Root password
rootPassword=
rootPasswordAlgorithm=sha512

# Auto-login
autologin=false
autologinGroup=wheel
